# CodeB Platform - 시스템 아키텍처 문서

## 📋 목차
- [개요](#개요)
- [시스템 아키텍처](#시스템-아키텍처)
- [기술 스택](#기술-스택)
- [프로젝트 구조](#프로젝트-구조)
- [데이터 흐름](#데이터-흐름)
- [보안 아키텍처](#보안-아키텍처)
- [확장성 고려사항](#확장성-고려사항)

---

## 개요

CodeB Platform은 Next.js 14 App Router 기반의 풀스택 프로젝트 관리 플랫폼입니다. Firebase를 백엔드로 사용하며, 실시간 협업 기능과 AI 통합을 제공합니다.

### 핵심 특징
- **프레임워크**: Next.js 14 (App Router)
- **언어**: TypeScript
- **백엔드**: Firebase (Authentication, Realtime Database, Storage)
- **실시간 통신**: Socket.io
- **상태 관리**: React Context API + Zustand
- **스타일링**: Tailwind CSS + Framer Motion

---

## 시스템 아키텍처

### 1. 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                        Client Layer                          │
├─────────────────────────────────────────────────────────────┤
│  Next.js 14 App Router                                       │
│  ├── Pages (RSC)                                             │
│  ├── Client Components (Interactive UI)                      │
│  └── Server Components (Data Fetching)                       │
└─────────────────────────────────────────────────────────────┘
                              ↓↑
┌─────────────────────────────────────────────────────────────┐
│                     Application Layer                        │
├─────────────────────────────────────────────────────────────┤
│  API Routes (/app/api/*)                                     │
│  ├── Authentication                                          │
│  ├── Projects Management                                     │
│  ├── Workspace & Board                                       │
│  ├── Attendance & Leave                                      │
│  └── Contracts & Transactions                                │
└─────────────────────────────────────────────────────────────┘
                              ↓↑
┌─────────────────────────────────────────────────────────────┐
│                       Service Layer                          │
├─────────────────────────────────────────────────────────────┤
│  ├── Auth Service (Firebase Auth)                           │
│  ├── Chat Service (Realtime DB)                             │
│  ├── AI Service (Google Gemini)                             │
│  ├── Notification Service                                    │
│  └── Socket Service (Real-time updates)                      │
└─────────────────────────────────────────────────────────────┘
                              ↓↑
┌─────────────────────────────────────────────────────────────┐
│                        Data Layer                            │
├─────────────────────────────────────────────────────────────┤
│  Firebase Services                                           │
│  ├── Realtime Database (Primary Data Store)                 │
│  ├── Storage (File Management)                              │
│  └── Authentication (User Management)                        │
│                                                              │
│  Prisma ORM (준비 중 - PostgreSQL 마이그레이션용)            │
└─────────────────────────────────────────────────────────────┘
```

### 2. 컴포넌트 아키텍처

```
src/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # 인증 라우트 그룹
│   ├── (dashboard)/              # 대시보드 라우트 그룹
│   ├── (admin)/                  # 관리자 라우트 그룹 (삭제됨)
│   ├── (customer)/               # 고객 라우트 그룹 (삭제됨)
│   ├── api/                      # API Routes
│   └── layout.tsx                # 루트 레이아웃
│
├── components/                   # UI 컴포넌트
│   ├── ui/                       # 기본 UI 컴포넌트 (Radix UI)
│   ├── layout/                   # 레이아웃 컴포넌트
│   ├── dashboard/                # 대시보드 컴포넌트
│   ├── projects/                 # 프로젝트 관련 컴포넌트
│   ├── chat/                     # 채팅 컴포넌트
│   ├── ai/                       # AI 관련 컴포넌트
│   ├── kanban/                   # 칸반 보드
│   ├── gantt/                    # 간트 차트
│   └── mindmap/                  # 마인드맵
│
├── lib/                          # 핵심 라이브러리
│   ├── firebase.ts               # Firebase 초기화
│   ├── auth-context.tsx          # 인증 컨텍스트
│   ├── chat-service.ts           # 채팅 서비스
│   └── prisma.ts                 # Prisma 클라이언트
│
├── services/                     # 비즈니스 로직 서비스
│   ├── ai-service.ts             # AI 통합
│   └── notification-service.ts   # 알림 서비스
│
└── types/                        # TypeScript 타입 정의
    └── index.ts
```

---

## 기술 스택

### Frontend
| 기술 | 버전 | 용도 |
|------|------|------|
| Next.js | 14.1.0 | React 프레임워크 (App Router) |
| React | 18.2.0 | UI 라이브러리 |
| TypeScript | 5.x | 타입 안정성 |
| Tailwind CSS | 3.4.0 | 유틸리티 CSS |
| Framer Motion | 10.18.0 | 애니메이션 |
| Radix UI | Latest | 접근성 UI 컴포넌트 |
| Zustand | 4.4.7 | 상태 관리 |

### Backend & Services
| 기술 | 버전 | 용도 |
|------|------|------|
| Firebase | 10.7.1 | BaaS (Auth, DB, Storage) |
| Firebase Admin | 13.4.0 | 서버 사이드 SDK |
| Socket.io | 4.8.1 | 실시간 통신 |
| Prisma | 5.10.0 | ORM (향후 PostgreSQL용) |
| Redis (ioredis) | 5.8.2 | 캐싱 & 세션 |

### AI & Analytics
| 기술 | 버전 | 용도 |
|------|------|------|
| Google Gemini | 0.24.1 | AI 어시스턴트 |
| Recharts | 2.10.4 | 데이터 시각화 |

### Development Tools
| 기술 | 버전 | 용도 |
|------|------|------|
| Jest | 29.7.0 | 테스트 프레임워크 |
| ESLint | 8.x | 코드 린팅 |
| Concurrently | 9.2.0 | 멀티 프로세스 실행 |

---

## 프로젝트 구조

### 라우트 구조

```
app/
├── (auth)/                       # 인증 관련 페이지
│   └── login/page.tsx
│
├── (dashboard)/                  # 메인 대시보드
│   ├── dashboard/page.tsx        # 대시보드 홈
│   ├── projects/                 # 프로젝트 관리
│   │   ├── page.tsx
│   │   └── [id]/page.tsx
│   ├── tasks/page.tsx            # 작업 관리
│   ├── gantt/page.tsx            # 간트 차트 (전역)
│   ├── messages/page.tsx         # 메시지
│   ├── files/page.tsx            # 파일 관리
│   ├── users/page.tsx            # 사용자 관리
│   ├── contracts/page.tsx        # 계약 관리
│   ├── invoices/page.tsx         # 인보이스
│   ├── deliverables/page.tsx     # 납품 관리
│   ├── automation/               # 자동화
│   │   ├── page.tsx
│   │   └── runs/page.tsx
│   ├── workspace/                # 워크스페이스
│   │   ├── board/page.tsx        # 보드
│   │   ├── calendar/page.tsx     # 캘린더
│   │   └── mindmap/page.tsx      # 마인드맵
│   ├── groupware/                # 그룹웨어
│   │   ├── attendance/page.tsx   # 근태 관리
│   │   ├── leave/page.tsx        # 휴가 관리
│   │   ├── organization/page.tsx # 조직도
│   │   └── announcement/page.tsx # 공지사항
│   ├── hr/                       # 인사 관리
│   │   ├── employees/page.tsx
│   │   └── payroll/page.tsx
│   └── finance/                  # 재무 관리
│       ├── contracts/page.tsx
│       └── pl/page.tsx           # 손익계산서
│
└── api/                          # API Routes
    ├── attendance/               # 근태 API
    ├── board/                    # 보드 API
    ├── contracts/                # 계약 API
    ├── leave/                    # 휴가 API
    ├── projects/                 # 프로젝트 API
    ├── transactions/             # 거래 API
    ├── workspace/                # 워크스페이스 API
    └── workspaces/               # 워크스페이스 목록 API
```

### 컴포넌트 구조

```
components/
├── ui/                           # Radix UI 기반 기본 컴포넌트
│   ├── button.tsx
│   ├── dialog.tsx
│   ├── dropdown-menu.tsx
│   ├── input.tsx
│   ├── select.tsx
│   ├── tabs.tsx
│   └── ...
│
├── layout/                       # 레이아웃 컴포넌트
│   ├── Sidebar.tsx               # 사이드바
│   ├── Header.tsx                # 헤더
│   └── Footer.tsx                # 푸터
│
├── auth/                         # 인증 관련
│   └── QuickLoginButtons.tsx     # 빠른 로그인
│
├── dashboard/                    # 대시보드
│   ├── DashboardStats.tsx        # 통계
│   ├── DashboardCharts.tsx       # 차트
│   ├── ProjectProgress.tsx       # 프로젝트 진행
│   └── Timeline.tsx              # 타임라인
│
├── projects/                     # 프로젝트
│   ├── ProjectCreateWizard.tsx   # 프로젝트 생성 마법사
│   ├── ProjectInvitations.tsx    # 초대 관리
│   └── ProjectSidebar.tsx        # 프로젝트 사이드바
│
├── chat/                         # 채팅
│   └── ChatWindow.tsx            # 채팅 창
│
├── kanban/                       # 칸반 보드
│   ├── KanbanBoardPro.tsx        # 프로 버전
│   └── KanbanBoardDnD.tsx        # 드래그 앤 드롭 버전
│
├── gantt/                        # 간트 차트
│   ├── GanttChartSimplified.tsx  # 간단 버전
│   ├── FrappeGanttChart.tsx      # Frappe 기반
│   └── SvarGanttChart.tsx        # Svar 기반
│
├── mindmap/                      # 마인드맵
│   ├── MindmapEditor.tsx         # 에디터
│   └── MindmapNode.tsx           # 노드
│
├── workspace/                    # 워크스페이스
│   ├── BoardView.tsx             # 보드 뷰
│   ├── CalendarView.tsx          # 캘린더 뷰
│   └── ListView.tsx              # 리스트 뷰
│
├── organization/                 # 조직 관리
│   ├── DepartmentColumn.tsx      # 부서 컬럼
│   ├── DepartmentManageModal.tsx # 부서 관리 모달
│   └── MemberCard.tsx            # 멤버 카드
│
└── ai/                           # AI 기능
    ├── AIAssistant.tsx           # AI 어시스턴트
    └── AIInsights.tsx            # AI 인사이트
```

---

## 데이터 흐름

### 1. 인증 플로우

```
User Input (Login Form)
    ↓
Firebase Authentication
    ↓
AuthContext Update
    ↓
Protected Route Check
    ↓
Dashboard Access
```

### 2. 프로젝트 데이터 플로우

```
Client Request
    ↓
Next.js API Route (/api/projects/*)
    ↓
Firebase Realtime Database
    ↓
Real-time Listeners (onValue)
    ↓
Component State Update
    ↓
UI Re-render
```

### 3. 실시간 채팅 플로우

```
User Message
    ↓
Socket.io Client Emit
    ↓
Socket.io Server
    ↓
Firebase Realtime DB (messages/)
    ↓
Socket.io Broadcast
    ↓
All Connected Clients Update
```

### 4. AI 서비스 플로우

```
User Query
    ↓
AI Service (/services/ai-service.ts)
    ↓
Google Gemini API
    ↓
Response Processing
    ↓
Context Update
    ↓
UI Display
```

---

## 보안 아키텍처

### 1. 인증 & 권한

```typescript
// 역할 기반 접근 제어 (RBAC)
type UserRole = 'admin' | 'manager' | 'developer' | 'customer';

// 권한 레벨
const rolePermissions = {
  admin: ['*'],                    // 모든 권한
  manager: ['read', 'write', 'update'],
  developer: ['read', 'write'],
  customer: ['read']
};
```

### 2. Firebase 보안 규칙

```json
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null",
        ".write": "$uid === auth.uid || root.child('users').child(auth.uid).child('role').val() === 'admin'"
      }
    },
    "projects": {
      "$projectId": {
        ".read": "auth != null",
        ".write": "root.child('projects').child($projectId).child('team').child(auth.uid).exists() || root.child('users').child(auth.uid).child('role').val() === 'admin'"
      }
    }
  }
}
```

### 3. API 보안

- **인증 미들웨어**: 모든 API 요청에 Firebase ID 토큰 검증
- **CORS 설정**: 허용된 도메인만 접근 가능
- **Rate Limiting**: API 남용 방지
- **입력 검증**: XSS, SQL Injection 방지

---

## 확장성 고려사항

### 1. 데이터베이스 마이그레이션 계획

```
현재 상태:
- Firebase Realtime Database (주요 데이터)
- Prisma ORM 설정 완료

향후 계획:
- PostgreSQL 마이그레이션
- Redis 캐싱 레이어 추가
- 복합 쿼리 최적화
```

### 2. 성능 최적화

- **코드 스플리팅**: Next.js 동적 import
- **이미지 최적화**: Next.js Image 컴포넌트
- **API 캐싱**: React Query + Redis
- **번들 최적화**: Tree shaking, Minification

### 3. 모니터링 & 로깅

```typescript
// 향후 통합 예정
- Sentry (에러 추적)
- Google Analytics (사용자 분석)
- Firebase Performance Monitoring
- Custom Logging Service
```

### 4. CI/CD 파이프라인

```
GitHub Actions
    ↓
Type Check → Lint → Test
    ↓
Build Production
    ↓
Deploy to Vercel
    ↓
Post-deployment Tests
```

---

## 향후 개발 계획

### Phase 1 (완료)
- ✅ Next.js 14 App Router 마이그레이션
- ✅ Firebase 통합
- ✅ 기본 CRUD 기능
- ✅ 실시간 채팅

### Phase 2 (진행 중)
- 🔄 Prisma + PostgreSQL 마이그레이션
- 🔄 Redis 캐싱 레이어
- 🔄 고급 AI 기능
- 🔄 E2E 테스트 커버리지 확대

### Phase 3 (계획)
- 📋 모바일 앱 (React Native)
- 📋 GraphQL API
- 📋 마이크로서비스 아키텍처
- 📋 Kubernetes 배포

---

**문서 버전**: 1.0.0
**최종 업데이트**: 2025-01-24
**작성자**: CodeB Development Team
