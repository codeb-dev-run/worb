# ==============================================================================
# worb - CI/CD Pipeline (Blue-Green Deployment v4.0.0)
# Generated by CodeB CLI v4.0.0
# ==============================================================================
# Blue-Green Deployment:
#   - Build: GitHub-hosted runner (ubuntu-latest)
#   - Deploy: MCP API (no SSH required)
#   - Strategy: Blue-Green slot switching for zero-downtime
# ==============================================================================

name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - staging
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: codeb-dev-run/worb

jobs:
  build:
    name: Build & Push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      image_tag: ${{ steps.meta.outputs.version }}
      environment: ${{ steps.vars.outputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set variables
        id: vars
        run: |
          echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            echo "environment=production" >> $GITHUB_OUTPUT
          else
            echo "environment=staging" >> $GITHUB_OUTPUT
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=staging,enable=${{ github.ref == 'refs/heads/staging' }}
            type=sha,prefix=,format=short

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy via MCP API
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Inactive Slot (Blue-Green)
        id: deploy
        run: |
          ENVIRONMENT="${{ needs.build.outputs.environment }}"
          IMAGE_TAG="${{ github.ref == 'refs/heads/main' && 'latest' || 'staging' }}"

          echo "🚀 Blue-Green 배포: worb → $ENVIRONMENT (비활성 슬롯)"

          RESPONSE=$(curl -sf -X POST "https://app.codeb.kr/api/tool" \
            -H "X-API-Key: ${{ secrets.CODEB_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"tool\": \"deploy\",
              \"params\": {
                \"project\": \"worb\",
                \"image\": \"ghcr.io/${{ env.IMAGE_NAME }}:$IMAGE_TAG\",
                \"environment\": \"$ENVIRONMENT\",
                \"strategy\": \"blue-green\"
              }
            }")

          echo "Deploy Response: $RESPONSE"

          if echo "$RESPONSE" | grep -q '"success":true'; then
            echo "✅ 비활성 슬롯 배포 완료!"
            SLOT=$(echo "$RESPONSE" | grep -o '"slot":"[^"]*"' | cut -d'"' -f4)
            echo "slot=$SLOT" >> $GITHUB_OUTPUT
          else
            echo "❌ 배포 실패"
            exit 1
          fi

      - name: Health Check (Deployed Slot)
        run: |
          echo "⏳ 헬스체크 대기 (10초)..."
          sleep 10

          SLOT="${{ steps.deploy.outputs.slot }}"
          if [ "$SLOT" = "blue" ]; then
            PREVIEW_PORT=4000
          else
            PREVIEW_PORT=4001
          fi

          curl -sf "http://158.247.203.55:$PREVIEW_PORT/api/health" && echo "✅ 헬스체크 통과!" || echo "⚠️ 헬스체크 대기 중..."

      - name: Auto Promote (Production Only)
        if: needs.build.outputs.environment == 'production'
        run: |
          echo "🔄 트래픽 전환 (Promote)..."

          RESPONSE=$(curl -sf -X POST "https://app.codeb.kr/api/tool" \
            -H "X-API-Key: ${{ secrets.CODEB_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{
              \"tool\": \"promote\",
              \"params\": {
                \"project\": \"worb\",
                \"environment\": \"production\"
              }
            }")

          if echo "$RESPONSE" | grep -q '"success":true'; then
            echo "✅ 트래픽 전환 완료! (worb.codeb.kr → ${{ steps.deploy.outputs.slot }})"
          else
            echo "⚠️ 트래픽 전환 실패 - 수동 promote 필요"
          fi

      - name: Final Health Check
        run: |
          sleep 5
          curl -sf "https://worb.codeb.kr/api/health" && echo "✅ 프로덕션 헬스체크 통과!" || echo "⚠️ 헬스체크 대기 중..."
